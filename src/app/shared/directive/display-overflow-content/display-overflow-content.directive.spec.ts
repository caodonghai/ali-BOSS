import { DisplayOverflowContentDirective } from './display-overflow-content.directive';

describe('DisplayOverflowContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayOverflowContentDirective();
    expect(directive).toBeTruthy();
  });
});

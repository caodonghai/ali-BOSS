import { FilterHtmlTagDirective } from './filter-html-tag.directive';

describe('FilterHtmlTagDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterHtmlTagDirective();
    expect(directive).toBeTruthy();
  });
});
